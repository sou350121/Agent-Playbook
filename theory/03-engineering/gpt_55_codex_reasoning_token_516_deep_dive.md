---
auto_generated: true
generated_at: "2026-07-10T12:43:00Z"
source_url: "https://github.com/openai/codex/issues/30364"
signal_type: "significant_update"
---
# GPT-5.5 Codex 推理 Token 聚类异常：516 阈值导致复杂任务性能下降

> 🔍 本文由 Moltbot 自动生成 | 2026-07-10
>
> **项目/工具**: OpenAI Codex (gpt-5.5)
> **链接**: https://github.com/openai/codex/issues/30364
> **核心定位**: 39 万条 Codex 遥测数据显示 GPT-5.5 在复杂编码任务中存在固定推理 token 阈值行为（516/1034/1552），44% 的响应卡在 516 token 即终止推理，伴随整体推理深度下降，可能导致复杂任务性能退化。

## ⚡ 快速判断（30 秒读完这段就够了）

- **一句话定位**: GPT-5.5 在 Codex 中的推理 token 分布出现异常聚类——近半数响应精确停在 516 token，疑似存在隐藏的推理预算上限或截断机制。
- **现在值得关注吗**: 是。如果你在用 Codex Desktop 跑复杂任务，GPT-5.5 可能比 GPT-5.2 表现更差——不是模型变笨了，而是推理被提前截断。
- **适合场景**: 简单/中等复杂度编码任务（516 token 推理足够覆盖的场景）
- **不适合场景**: 复杂多步骤推理任务（架构重构、跨文件调试、深层依赖分析），GPT-5.5 在此类任务上可能因推理截断给出错误答案
- **与 GPT-5.2/5.4 核心差异**: GPT-5.5 的 exact-516 比率是 GPT-5.2 的 ~130 倍（44.0% vs 0.34%），是 GPT-5.4 的 ~2.2 倍（44.0% vs 19.8%）

## 是什么 / 解决什么问题

OpenAI Codex 是一个 AI 编码代理工具，用户通过自然语言指令驱动它完成编码任务。Codex 内部使用推理模型（reasoning model）进行思考，推理过程消耗 reasoning tokens，最终输出结果。

2026 年 2-6 月的 390,195 条 Codex 遥测数据揭示了一个异常模式：**GPT-5.5 的推理 token 计数不是自然分布的，而是高度集中在几个固定值上**——特别是 516、1034 和 1552。其中 516 是最显著的聚类点。

这不是一个孤立的 bug 报告。相关 issue [#29353](https://github.com/openai/codex/issues/29353) 已经在任务级别复现了这个问题：当 GPT-5.5 的推理恰好停在 516 token 时，返回的答案往往是错误的。本次分析将证据从单个任务扩展到 865 个 session、39 万条响应记录，确认这是一个系统性的模型行为异常。

## 技术架构拆解

### 核心发现：三层证据链

**第一层：模型特异性**

GPT-5.5 只占所有响应的 19.3%，却贡献了 82.0% 的 exact-516 事件。各模型的 exact-516 / >=516 比率对比：

| 模型 | 响应记录数 | exact-516 / >=516 比率 |
|------|-----------|----------------------|
| gpt-5.5 | 75,401 | **44.0%** |
| gpt-5.4 | 25,214 | 19.8% |
| gpt-5.2 | 247,575 | 0.34% |
| gpt-5.3-codex | 13,333 | 0.0% |
| gpt-5.3-codex-spark | 26,179 | 0.0% |

GPT-5.5 的聚类强度是 GPT-5.2 的 **~130 倍**，是 GPT-5.3 系列变体的 **无限倍**（后者完全没有此现象）。

**第二层：时间趋势——急剧恶化**

| 月份 | exact-516 / >=516 比率 | 平均推理 token 数 | P90 推理 token 数 |
|------|----------------------|-----------------|-----------------|
| Feb 2026 | 0.11% | 268.1 | 772 |
| Mar 2026 | 2.45% | 256.8 | 723 |
| Apr 2026 | 4.25% | 228.7 | 669 |
| May 2026 | **53.30%** | **106.9** | **344** |
| Jun 2026 | 35.84% | 168.5 | 515 |

5 月是转折点——聚类比率从 4% 飙升到 53%，同时平均推理 token 数从 229 暴跌到 107。6 月略有回落但仍远高于基线。

**第三层：固定阈值模式**

516、1034、1552 这三个值不是随机出现的——它们看起来像 **重复的阈值边界**（516 × 1, 516 × 2 + 2, 516 × 3 + 4）。自然分布的推理 token 计数不应该在这些精确值上形成尖峰。

### 架构/信息流图

```
Codex 推理流程（推测）
─────────────────────────────────────────────

用户任务 → [路由层] → 模型选择 (gpt-5.5)
                      ↓
              [推理预算分配器] ←── ??? 516/1034/1552 阈值？
                      ↓
              [推理引擎] 生成 reasoning tokens
                      ↓
         ┌────────────┴────────────┐
         ↓                         ↓
   自然终止                  达到阈值截断
   (复杂度驱动)               (516/1034/1552)
         ↓                         ↓
    高质量答案              低质量/错误答案
                            (#29353 已复现)
```

### 与前版的关键差异

| 维度 | GPT-5.2 | GPT-5.4 | GPT-5.5 |
|------|---------|---------|---------|
| exact-516 比率 | 0.34% | 19.8% | **44.0%** |
| 平均推理 token (Jun) | 未报告 | 未报告 | 168.5 |
| P90 推理 token (Jun) | 未报告 | 未报告 | 515 |
| 5 月聚类飙升 | 否 | 未报告 | **是 (53%)** |
| 复杂任务准确性 | 基线 | 中等 | **退化** |

### 可能的机制假设

报告作者明确表示 **"不声称这证明了隐藏的 chain-of-thought 截断"**，但提出了几种可能的内部机制：

1. **推理预算上限（Reasoning Budget Cap）**: GPT-5.5 可能被分配了一个固定的推理 token 预算（~516），达到后强制终止推理
2. **路由/降级行为（Routing/Fallback）**: 某些任务类型被路由到低推理模式
3. **调度器行为（Scheduler Behavior）**: 内部调度器在特定 token 计数点触发截断
4. **训练/对齐副作用**: GPT-5.5 的对齐训练可能无意中教会了模型"在 ~516 token 处停止"

## 实用评估

### 什么场景值得用 GPT-5.5

- **简单代码生成**: 函数级代码生成、单文件修改，516 token 推理通常足够
- **快速问答**: 概念解释、API 查询等不需要深度推理的任务
- **代码审查（浅层）**: 检查代码风格、简单 bug 检测

### 什么场景不值得用

- **复杂架构重构**: 跨多文件、需要理解深层依赖关系的重构任务——516 token 的推理深度明显不足
- **跨文件调试**: 需要追踪 bug 在多个模块间传播路径的场景
- **深层依赖分析**: 理解复杂代码库的架构决策和隐含依赖
- **高可靠性要求的任务**: #29353 已证实 exact-516 响应返回错误答案的概率显著更高

### 迁移建议

| 从 | 到 | 建议 |
|---|---|---|
| GPT-5.5 (复杂任务) | GPT-5.2 | 如果复杂任务准确率低，回退到 5.2——它的推理分布更自然，exact-516 比率仅 0.34% |
| GPT-5.5 (简单任务) | 保持 | 简单任务上 5.5 的聚类不影响质量 |
| GPT-5.4 | 观望 | 5.4 已有 19.8% 聚类，但不如 5.5 严重 |

## 对你的意义

这个发现对 AI 编码工具的选择有直接指导意义：

1. **模型版本不是越新越好**: GPT-5.5 在 Codex 中对于复杂任务可能比 GPT-5.2 更差——这是一个罕见的"新版本退化"的量化证据
2. **推理深度是可观测的信号**: 通过 token_count 元数据可以间接推断模型的内部行为，这为 AI 编码工具的 QA 提供了一种新的监控维度
3. **对 Agent 架构的启示**: 如果你在用其他 AI 编码框架（Cursor、Claude Code 等），应该检查它们的遥测数据是否也有类似的聚类异常

**建议**: 如果你在用 Codex Desktop 跑复杂任务，暂时回退到 GPT-5.2 或 GPT-5.4，直到 OpenAI 修复或解释这个行为。对于简单任务，GPT-5.5 仍然可用。

## 关键代码/配置片段

以下是报告中的核心验证查询逻辑（伪代码形式）：

```python
# 验证 exact-516 聚类
query = """
SELECT 
    model,
    DATE(timestamp) as day,
    COUNT(*) as total_responses,
    SUM(CASE WHEN reasoning_output_tokens = 516 THEN 1 ELSE 0 END) as exact_516,
    SUM(CASE WHEN reasoning_output_tokens >= 516 THEN 1 ELSE 0 END) as gte_516,
    SUM(CASE WHEN reasoning_output_tokens = 516 THEN 1 ELSE 0 END) * 100.0 / 
        NULLIF(SUM(CASE WHEN reasoning_output_tokens >= 516 THEN 1 ELSE 0 END), 0) 
        as exact_516_ratio
FROM codex_token_count
WHERE timestamp BETWEEN '2026-02-01' AND '2026-06-27'
GROUP BY model, DATE(timestamp)
ORDER BY day DESC
"""

# 对比模型推理强度趋势
monthly_stats = """
SELECT 
    DATE_TRUNC('month', timestamp) as month,
    model,
    AVG(reasoning_output_tokens) as mean_tokens,
    PERCENTILE_CONT(0.90) WITHIN GROUP (ORDER BY reasoning_output_tokens) as p90_tokens
FROM codex_token_count
GROUP BY month, model
"""
```

---
[← Back to Deep Dives](./README.md)

## 📌 AI Agent 假设追踪

| 假设 | 方向 | 关联说明 |
|------|------|----------|
| A-004: 推理模型在 Agent 任务展现持续优势 | 挑战 | GPT-5.5 作为更新的推理模型，在 Codex 复杂任务上因疑似推理截断反而表现更差——推理模型的优势并非单调递增，内部机制（预算/截断）可能抵消模型能力增益 |
