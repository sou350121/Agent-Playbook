---
auto_generated: true
generated_at: "2026-05-28T03:38:09Z"
source_url: "https://www.36kr.com/p/3826964226691972"
signal_type: "blog_post"
---
# 小米 MiMo 加入 API 价格战：永久降价 99%，全面对标 DeepSeek
(Xiaomi MiMo Joins API Price War: Permanent 99% Cut, Directly Competing with DeepSeek)

> 🔍 本文由 Moltbot 自动生成 | 2026-05-28
>
> **项目/工具**: 小米 MiMo-V2.5 系列大模型
> **链接**: https://www.36kr.com/p/3826964226691972
> **核心定位**: 国内大模型 API 价格战进入新阶段 — 小米 MiMo 以永久降价 99% + Token Plan 5-8 倍扩容直接对标 DeepSeek 地板价，背后是 SGLang HiCache + SWA 推理系统优化的成本优势

## ⚡ 快速判断（30 秒读完这段就够了）

- **一句話定位**：小米 MiMo-V2.5 系列 API 永久降价最高 99%，取消按上下文长度定价，Token Plan 套餐容量提升 5-8 倍，直接对标 DeepSeek 当前 API 价格
- **现在值得用吗**：是 — 如果你是 Agent/Code 场景用户，高缓存命中下 Lite 档（39 元/月）可跑 5 亿 Token 以上，性价比极高
- **适合场景**：Agent 推理（缓存命中率高）、代码生成、长文本处理（统一 1M 上下文定价）、高并发批量推理
- **不适合场景**：需要顶级推理能力的复杂任务（MiMo-V2.5-Pro 虽强但非绝对第一）、对数据隐私有严格要求的场景（云 API 模式）
- **与 DeepSeek 核心差异**：价格已全面对齐 DeepSeek，但 MiMo 以 MIT 协议开源，支持本地部署与二次训练，DeepSeek 的开源策略相对保守

## 是什么 / 解决什么问题

2026 年 5 月底，国内大模型 API 价格战再次升温。短短一周内，DeepSeek 和小米 MiMo 先后宣布永久降价。DeepSeek 在 5 月下旬率先将 V4-Pro 促销价转为正式价，并把输入缓存命中价格压到原价 1/10。紧接着，小米 MiMo 于 5 月 27 日宣布 MiMo-V2.5 系列 API 永久下调，最高降幅 99%，且不再区分 256K 与 1M 上下文窗口的定价。

这背后的核心逻辑很清晰：当模型能力进入前排后（MiMo-V2.5-Pro 在 Artificial Analysis 综合智能榜位列全球开源模型并列第一），价格成为获取开发者的关键杠杆。小米此前通过"100 万亿 Token 免费计划"已将 Hermes Agent 推上 OpenRouter 全球调用量榜首，但榜单变化极快 — 截至 5 月 27 日，MiMo-V2.5-Pro 已从榜首跌至第 14 名。永久降价 + Token Plan 扩容，是小米在调用量流失前的防守反击。

更深层看，这场价格战的底气来自推理系统优化。小米团队基于 SGLang HiCache 完整支持 SWA（Sliding Window Attention），将 KV Cache 在 GPU 显存、CPU 内存、SSD 等多级存储间的数据搬运量降至优化前的约 1/7，可缓存 Token 数量提升至约 5 倍。这意味着单位 Token 的推理成本大幅下降，使得 99% 降价在商业上可行。

## 技术架构拆解

### 核心设计决策

| 决策点 | 具体做法 | 理由 |
|--------|---------|------|
| 永久降价 99% | 缓存命中 0.02 元/百万 tokens，输入 1 元，输出 2 元 | 对标 DeepSeek 地板价，锁定价格敏感型开发者 |
| 取消上下文长度差异化定价 | 256K 与 1M 上下文统一价格 | 简化计费逻辑，鼓励用户使用长上下文而不必担心成本 |
| Token Plan 扩容 5-8 倍 | 价格不变，Credits 大幅提升 | 在价格战之外提供差异化价值，增强用户粘性 |
| 高缓存命中场景优化 | Agent/Code 场景缓存命中率更高，实际 Token 量更大 | 精准定位主力使用场景，放大性价比感知 |
| MIT 协议开源 | 支持商用部署与二次训练，无需额外授权 | 降低采用门槛，与闭源/半开源竞品形成差异化 |

### 价格对比：MiMo vs DeepSeek

| 维度 | DeepSeek | 小米 MiMo-V2.5 | 小米 MiMo-V2.5-Pro |
|------|----------|----------------|---------------------|
| 缓存命中价格 | ~0.02 元/百万 tokens | 0.02 元/百万 tokens | 0.025 元/百万 tokens |
| 未命中输入 | ~1 元/百万 tokens | 1 元/百万 tokens | 3 元/百万 tokens |
| 输出价格 | ~2 元/百万 tokens | 2 元/百万 tokens | 6 元/百万 tokens |
| 上下文差异化定价 | 有 | **无（统一价）** | **无（统一价）** |
| 开源协议 | 部分开源 | **MIT 全开源** | **MIT 全开源** |
| 本地部署支持 | 有限 | ✅ 完整支持 | ✅ 完整支持 |

### 推理系统优化架构

```
┌─────────────────────────────────────────────────────┐
│                  推理请求层                          │
│  Agent / Code / 长文本 → 输入分桶策略优化            │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│              SGLang HiCache 核心引擎                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  GPU 显存    │←→│  CPU 内存    │←→│   SSD       │  │
│  │  (热 Cache)  │  │  (温 Cache)  │  │  (冷 Cache)  │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  │
│                                                      │
│  SWA (Sliding Window Attention) → KV Cache 压缩      │
│  数据搬运量 ↓ 至 ~1/7  |  可缓存 Token ↑ 至 ~5x      │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│              专家并行 + 吞吐优化                      │
│  集群输入吞吐 ↑ → 单位 Token 成本 ↓                  │
│  → 支撑 99% 降价的商业可行性                         │
└─────────────────────────────────────────────────────┘
```

### 与前版的关键差异

| 维度 | MiMo 旧版 | MiMo-V2.5 新版 |
|------|-----------|----------------|
| 缓存命中价格 | 约 1-2 元/百万 tokens | 0.02 元/百万 tokens（↓99%） |
| 上下文定价 | 按 256K / 1M 区分 | 统一价格 |
| Token Plan 容量 | 基准值 | 5-8 倍提升 |
| 推理后端 | 标准 SGLang | SGLang HiCache + SWA |
| 开源协议 | 有限开源 | MIT 全开源 |

## 实用评估

### 什么场景值得用

- **Agent 推理**：Agent 场景缓存命中率通常极高（重复的系统 prompt + 工具调用模式），39 元 Lite 档在高缓存命中下可跑 5 亿 Token 以上，99 元 Standard 档超 13 亿 Token。对于需要大量 Agent 调用的项目，这是目前市场上最具性价比的选择之一。
- **代码生成**：Code 场景同样具有高缓存命中特征（代码模板、框架代码高度重复），实际可用 Token 量会明显高于标称值。
- **长文本处理**：取消上下文长度差异化定价后，处理 1M 上下文不再需要额外付费，适合文档分析、长对话等场景。
- **本地部署 + 二次训练**：MIT 协议意味着可以自由商用和二次训练，适合需要数据本地化或领域定制的团队。

### 什么场景不值得用

- **需要绝对最强推理能力的任务**：MiMo-V2.5-Pro 虽位列开源第一梯队，但 OpenRouter 本周榜单前三名是 DeepSeek-V4-Flash、Owl Alpha、DeepSeek-V4-Pro。对于极限推理任务，可能仍需考虑其他模型。
- **对数据隐私有极高要求的场景**：云 API 模式意味着数据经过第三方服务器，金融、医疗等强监管行业需评估合规风险。
- **对 API 稳定性要求极高的生产环境**：小米 MiMo 在 AI 领域仍是相对新玩家，SLO 保障、SLA 承诺、故障恢复能力等尚未经历大规模长期验证。

### 迁移成本

| 迁移路径 | 工作量 | 说明 |
|----------|--------|------|
| 从 DeepSeek API 迁移 | 低 | 价格已对齐，主要是修改 endpoint 和 API key；需注意上下文定价逻辑变化 |
| 从 OpenRouter 迁移 | 中 | OpenRouter 支持多模型路由，迁移到 MiMo 直连需调整路由逻辑 |
| 从其他国产模型迁移 | 中-高 | 取决于原模型的 API 兼容性；MiMo 的 API 接口需确认与现有代码的适配度 |
| 本地部署（开源版） | 高 | 需要 GPU 资源 + SGLang 部署经验，但 MIT 协议无授权障碍 |

## 对你的意义

这场价格战对你（AI 应用开发者）的直接意义：

1. **Agent 开发的推理成本可大幅下降**：如果你的 Agent 框架（如 LangChain、CrewAI）依赖云 API，切换到 MiMo 在高缓存命中场景下成本可降低 1-2 个数量级。
2. **本地部署门槛降低**：MIT 协议 + 开源，意味着你可以在自己的 GPU 上部署 MiMo-V2.5 系列，结合 SGLang HiCache 优化，本地推理成本可能低于云 API。
3. **价格战还在继续**：近半年已有小米、字节、阿里、智谱、腾讯等 5 家厂商调整套餐。趋势是两极分化 — 一方面地板价越来越低（DeepSeek、MiMo），另一方面部分厂商缩减低价套餐（阿里暂停 Coding Plan Lite、字节下架低价套餐、智谱 Q1 定价提升 83%）。选择 MiMo 或 DeepSeek 是当前成本最优解。

**建议**：如果你的 Agent 项目依赖高缓存命中场景（系统 prompt 固定、工具调用模式重复），立即试用 MiMo Lite 档（39 元/月）做 POC 验证。如果缓存命中率能达到 95%，实际 Token 量可能远超预期。

## 关键数据/配置片段

### MiMo-V2.5 API 价格表（来自 36kr 报道）

```
MiMo-V2.5:
  缓存命中:  0.02 元/百万 tokens
  未命中输入: 1.00 元/百万 tokens
  输出:       2.00 元/百万 tokens

MiMo-V2.5-Pro:
  缓存命中:  0.025 元/百万 tokens
  未命中输入: 3.00 元/百万 tokens
  输出:       6.00 元/百万 tokens
```

### Token Plan 高缓存命中场景 Token 估算（95%+ 缓存命中）

```
MiMo-V2.5:
  Lite (39元):    ≥ 5 亿 Token
  Standard (99元): ≥ 13 亿 Token
  Pro (329元):    ≥ 47 亿 Token
  Max (659元):    ≥ 100 亿 Token

MiMo-V2.5-Pro:
  Lite (39元):    ≥ 1.9 亿 Token
  Standard (99元): ≥ 5 亿 Token
  Pro (329元):    ≥ 18 亿 Token
  Max (659元):    ≥ 40 亿 Token
```

### 推理优化关键技术（官方披露）

> 小米团队基于 SGLang HiCache 完整支持 SWA（Sliding Window Attention），将 KV Cache 在 GPU 显存、CPU 内存、SSD 等多级存储间的数据搬运量降低至优化前的约 1/7，可缓存 Token 数量提升至约 5 倍。

### 行业背景：近半年国产模型定价调整

| 厂商 | 调整方向 | 具体动作 |
|------|----------|----------|
| 小米 MiMo | ↓ 降价 | 永久降价 99%，Token Plan 扩容 5-8x |
| DeepSeek | ↓ 降价 | V4-Pro 促销价转正式价，缓存命中降至原价 1/10 |
| 智谱 | ↑ 涨价 | Q1 API 调用定价提升 83% |
| 阿里 | 缩减低价 | 暂停 Coding Plan Lite |
| 字节 | 缩减低价 | 下架低价 Coding Plan |
| 腾讯 | 低价竞争 | Hy 仍有 28 元/月 Lite 档 |

## 📌 AI Agent 假设追踪

| 假设 | 方向 | 关联说明 |
|------|------|----------|
| A-005: AI 工作流自动化成为企业 AI 最快增长场景 | 支持 | 价格战使 Agent 推理成本降低 1-2 个数量级，直接消除企业大规模部署 Agent 工作流的成本障碍；小米特别强调 Agent/Code 场景下缓存命中率更高、实际 Token 量更大 |

---
[← Back to Deep Dives](./README.md)
