# Cursor Prompt: 改造 agent-experience-library 为 AI 应用开发监控系统

## 背景说明

**当前状态**: `agent-experience-library` 是一个 Agent 经验库，记录使用心得、工具方法论等。

**目标状态**: 将其改造为一套**AI应用开发自动化监控系统**的知识库，对应 [AI应用开发handbook设计](/mnt/user-data/outputs/ai_app_handbook_design.md) 中描述的架构。

## 核心改造任务

### 任务 1: 重新定位核心内容

**从**：Agent 使用经验和最佳实践
**到**：AI 应用生态的自动化监控与知识沉淀

**具体调整**:

1. **app_index.md 替代现有索引**
   - 创建 `stack/app_index.md` 作为核心索引文件
   - 分类建议：
     * Agent 框架 (LangChain, AutoGPT, CrewAI...)
     * UI/UX 工具 (聊天界面, 可视化编辑器...)
     * RAG 工具链 (向量数据库, 检索增强...)
     * API 包装器 (Claude, OpenAI, Anthropic...)
     * 垂直应用 (写作助手, 代码生成, 数据分析...)
     * 基础设施 (部署, 监控, 评估...)

2. **标注系统**
   - 实现两层标记系统:
     * 第一层: ⚡(战略级) / 🔧(可操作) / 📖(值得了解)
     * 第二层: 🎯(primary方向) / [方向名](team方向)
   - 示例格式:
     ```markdown
     | 应用/工具 | 开发者 | 日期 | 标籤 | 連結 | 備註 |
     |----------|--------|------|------|------|------|
     | Claude API | Anthropic | 2026-02-10 | API | https://... | 🎯🔧 daily 2026-02-10 |
     | LangChain v2 | LangChain | 2026-02-09 | Framework | https://... | [RAG] ⚡ daily 2026-02-09 |
     ```

### 任务 2: 重组目录结构

**新目录结构**:
```
agent-experience-library/
├── stack/                        # 技术层 (原 docs/)
│   ├── app_index.md             # ← 核心文件，Moltbot 每日写入
│   ├── technology_landscape.md  # 技术全景图
│   ├── frameworks/              # Agent 框架深度笔记
│   ├── tools/                   # 工具链分析
│   └── code-notes/              # 代码分析速记
│
├── implementation/              # 实作层 (保留部分 docs/tools/)
│   ├── api-integration/
│   ├── deployment/
│   └── architecture/
│
├── reports/                     # 时间层 (新建)
│   └── biweekly/
│       ├── README.md            # 报告索引
│       └── 2026-02-XX.md        # 双周报告
│
├── products/                    # 产品层 (原 companies/ 扩展)
│   ├── anthropic.md
│   ├── openai.md
│   └── ...
│
├── use-cases/                   # 场景层 (原 case-studies/ 转型)
│   ├── writing-assistant/
│   ├── code-generation/
│   └── data-analysis/
│
├── monitoring-system/           # 新增：自动化系统配置
│   ├── active-config.json       # 动态参数
│   ├── daily-stats/             # 统计数据
│   └── change-log/              # 进化记录
│
├── blog/                        # 保留，转为系统运行日志
├── starter-kits/                # 保留
├── .claude/                     # 保留
├── AGENT.md                     # 更新 (见任务 3)
└── README.md                    # 更新 (见任务 4)
```

**迁移指南**:
- `docs/beginner-guide/` → `implementation/getting-started/`
- `docs/architecture-governance/` → `implementation/architecture/`
- `docs/tools/` → 拆分到 `stack/tools/` (分析) 和 `implementation/` (实作)
- `docs/capabilities/` → `stack/frameworks/` (重命名为框架能力)
- `docs/prompt-library/` → `implementation/prompts/`
- `docs/case-studies/` → `use-cases/` (转为应用场景)
- `docs/planning/` → 保留核心文章，移到 `stack/methodology/`

### 任务 3: 更新 AGENT.md

**新的 AGENT.md 内容框架**:

```markdown
# Agent 指令：AI 应用开发监控系统

## 项目定位

这是一个 AI 应用开发生态的自动化监控与知识沉淀系统。目标是将每日涌入的 AI 工具、应用、API 更新转化为可查询、可推理、可累积的结构化知识资产。

## 核心工作流

### 1. 信号收集 (你不需要做)
- Moltbot 自动从 Product Hunt、GitHub、HN 等源收集信息
- 每日写入 `stack/app_index.md`
- Commit message 格式: `📱 daily apps: {日期} (+N apps)`

### 2. 人工深度生产 (你的角色)
- 在 `implementation/` 或 `stack/` 创建深度实作/分析笔记
- 使用 ✍️ 标记手动添加的内容
- Commit message: 普通格式，无 emoji 前缀

### 3. 双周推理 (Moltbot)
- 读取 Git diff，生成跨内容推理报告
- 输出到 `reports/biweekly/`

## 文件写入规则

### ✅ 允许编辑
- `implementation/**/*.md` - 实作指南
- `stack/frameworks/*.md` - 框架深度分析
- `stack/methodology/*.md` - 方法论文章
- `use-cases/**/*.md` - 应用场景案例
- `products/*.md` - 产品/公司分析

### ⚠️ 仅追加，不覆盖
- `stack/app_index.md` - 只能在表格末尾追加新行
- `reports/biweekly/README.md` - 只能追加索引条目

### ❌ 不要触碰
- `monitoring-system/active-config.json` - Moltbot 管理
- `monitoring-system/daily-stats/**` - 自动生成
- 任何带 📱/📊/📝 emoji 的 commit

## 标注系统

### 应用重要性
- ⚡ 战略级: 知名团队 + 架构创新，或大规模应用
- 🔧 可操作: 有开源代码/API/文档可复现
- 📖 值得了解: 有参考价值但不需立即行动

### 开发方向
- 🎯 primary 方向 (主攻，如 "Agent + UI")
- [方向名] team 方向 (信号追蹤，如 RAG、多模态)
- ✍️ 人工手动添加

### 组合示例
```markdown
🎯🔧 [AgentUI-Pro] Anthropic 發布新 Claude UI SDK...
[RAG] ⚡ LangChain 發布 RAG pipeline v2...
```

## 代码规范

### Markdown
- 使用标准 Markdown，不使用 MDX (除非文件已是 .mdx)
- 表格必须对齐
- 链接使用相对路径

### 文件命名
- 小写 + 连字符: `agent-ui-frameworks.md`
- 日期格式: `YYYY-MM-DD.md`
- 避免空格和特殊字符

### Commit Message
- 人工 commit: 描述性标题 (无 emoji)
- 示例: `Add Claude API integration guide`
- 避免: `update`, `fix`, `wip`

## 推荐工作流

### 添加新应用到索引
1. 打开 `stack/app_index.md`
2. 找到对应分类表格
3. 在表格末尾追加新行:
   ```markdown
   | [App Name] | Developer | 2026-02-11 | tag1, tag2 | [link] | ✍️ 手动加入 |
   ```

### 创建深度分析
1. 在 `stack/frameworks/` 或 `implementation/` 创建新文件
2. 使用描述性文件名
3. 包含以下章节:
   - 概述
   - 核心能力
   - 使用场景
   - 代码示例
   - 局限性
   - 参考资源

### 更新应用场景
1. 在 `use-cases/` 下创建场景文件夹
2. 包含:
   - `README.md` - 场景描述
   - `tech-stack.md` - 技术栈选择
   - `implementation.md` - 实现指南
   - `examples/` - 代码示例

## 查询提示

当用户询问:
- "最近有什么 X 相关的应用?" → 查 `stack/app_index.md`
- "如何实现 Y?" → 查 `implementation/` 和 `use-cases/`
- "Z 框架的能力?" → 查 `stack/frameworks/`
- "最近趋势?" → 查 `reports/biweekly/`

## 进阶技巧

- 使用 Git blame 查看条目来源
- 搜索 `备註` 列查看 Moltbot 标注逻辑
- 双周报告会总结跨分类趋势
- 检查 `monitoring-system/change-log/` 了解系统进化历史
```

### 任务 4: 更新 README.md

**新的 README.md 重点**:

```markdown
# AI 应用开发监控系统

> 🚀 将每日涌入的 AI 工具、应用、框架自动转化为可查询、可推理、可累积的知识资产

## 这是什么

一套自动化监控系统 + 结构化知识库，解决 AI 应用开发者的核心痛点:
- ✅ 每天有大量新工具发布，但没时间追踪
- ✅ 看过的内容无法累积，没有复利
- ✅ 需要时想不起来在哪看过

## 工作原理

```
每日自动收集 → 智能过滤 → 写入索引 → 双周推理 → 洞察报告
     ↓              ↓            ↓           ↓          ↓
Product Hunt    关键词过滤   app_index.md  Git diff  趋势分析
GitHub                                               机会窗口
HN/Reddit
```

## 快速开始

### 查应用
直接打开 [`stack/app_index.md`](stack/app_index.md)，按分类查找

### 看趋势
查看 [`reports/biweekly/`](reports/biweekly/) 最新报告

### 学实作
浏览 [`implementation/`](implementation/) 和 [`use-cases/`](use-cases/)

## 核心特性

- 📱 **自动索引**: Moltbot 每日收集，标注，写入
- 🎯 **智能过滤**: 两层标记系统 (重要性 + 方向)
- 📊 **趋势推理**: 双周跨内容分析
- 🔧 **可操作**: 实作指南 + 代码示例
- 🧠 **自我进化**: 系统自动调参优化

## 目录导航

| 目录 | 说明 | 更新频率 |
|------|------|---------|
| `stack/` | 技术栈分析、应用索引 | 每日 + 不定期 |
| `implementation/` | 实作指南、最佳实践 | 不定期 |
| `reports/` | 双周趋势报告 | 每两周 |
| `use-cases/` | 应用场景案例 | 不定期 |
| `products/` | 产品/公司分析 | 不定期 |

## 贡献指南

详见 [CONTRIBUTING.md](CONTRIBUTING.md)

## 许可协议

内容采用 CC BY 4.0，代码采用 MIT
```

### 任务 5: 创建关键配置文件

#### 创建 `monitoring-system/active-config.json`

```json
{
  "version": 1,
  "last_updated": "2026-02-11",
  "focus_areas": {
    "primary": "agent-ui",
    "team": ["RAG", "multi-modal", "evaluation"]
  },
  "keywords": {
    "A_tier": [
      "claude api",
      "agent framework",
      "langchain",
      "rag",
      "vector database"
    ],
    "B_tier": [
      "streaming",
      "real-time",
      "ui components",
      "chat interface",
      "prompt management"
    ]
  },
  "data_sources": {
    "product_hunt": {
      "enabled": true,
      "categories": ["AI", "Developer Tools"]
    },
    "github_trending": {
      "enabled": true,
      "languages": ["Python", "TypeScript", "JavaScript"]
    },
    "hacker_news": {
      "enabled": true,
      "min_score": 50
    }
  },
  "filtering_rules": {
    "primary_direction_threshold": "low",
    "team_direction_threshold": "high",
    "max_apps_per_day": 8,
    "always_include_priorities": true
  }
}
```

#### 创建 `monitoring-system/README.md`

```markdown
# 监控系统配置

## 文件说明

- `active-config.json` - 当前生效的配置参数
- `daily-stats/` - 每日统计数据
- `change-log/` - 配置变更历史

## 配置结构

### focus_areas
定义监控重点方向
- `primary`: 主攻方向，降低过滤门槛
- `team`: 团队方向，只关注大事

### keywords
两级关键词系统
- `A_tier`: 核心关键词，不可自动删除
- `B_tier`: 次级关键词，可自动调整

### data_sources
数据源配置

### filtering_rules
过滤规则

## 自我进化机制

每 14 天，系统自动:
1. 分析 `daily-stats/`
2. 调整 `B_tier` 关键词
3. 更新 `active-config.json`
4. 记录到 `change-log/`

## 人工干预

如需调整:
1. 编辑 `active-config.json`
2. 提交 commit: `chore: update monitoring config - [原因]`
3. 系统下次执行时生效
```

## 执行指南

### Phase 1: 备份与准备 (第 1 天)
```bash
# 创建备份分支
git checkout -b backup-before-restructure

# 创建新结构分支
git checkout -b restructure-to-monitoring-system
```

### Phase 2: 创建新目录结构 (第 1-2 天)
```bash
# 创建新目录
mkdir -p stack/{frameworks,tools,code-notes,methodology}
mkdir -p implementation/{getting-started,architecture,prompts,api-integration,deployment}
mkdir -p reports/biweekly
mkdir -p monitoring-system/{daily-stats,change-log}
mkdir -p use-cases

# 创建核心文件
touch stack/app_index.md
touch stack/technology_landscape.md
touch monitoring-system/active-config.json
touch monitoring-system/README.md
```

### Phase 3: 迁移内容 (第 2-3 天)

**自动化脚本建议**:
```bash
# 迁移脚本 (需要根据实际内容调整)
mv docs/tools/*.md stack/tools/
mv docs/capabilities/*.md stack/frameworks/
mv docs/case-studies/*.md use-cases/
# ... 更多迁移命令
```

**手动检查**:
- 确保所有链接更新
- 验证图片路径
- 检查交叉引用

### Phase 4: 更新文档 (第 3 天)
- 更新 AGENT.md
- 更新 README.md
- 更新 CONTRIBUTING.md
- 创建迁移指南文档

### Phase 5: 验证与清理 (第 4 天)
```bash
# 检查所有链接
find . -name "*.md" -exec grep -l "docs/" {} \;

# 验证新结构
tree -L 2

# 测试 Cursor 读取
# (在 Cursor 中询问: "这个项目是做什么的?")
```

## 成功标准

- [ ] `stack/app_index.md` 存在且格式正确
- [ ] 所有旧链接已更新或重定向
- [ ] AGENT.md 包含新的工作流指引
- [ ] README.md 清晰说明新定位
- [ ] `monitoring-system/` 配置文件就位
- [ ] 至少 5 个应用已按新格式录入索引
- [ ] Cursor 能正确理解项目定位
- [ ] 所有图片和静态资源可访问

## 渐进式改造建议

如果觉得一次性改造太大，可以分阶段:

**阶段 1 (最小可行)**: 
- 创建 `stack/app_index.md`
- 更新 AGENT.md
- 开始录入应用

**阶段 2 (结构优化)**:
- 重组目录
- 迁移内容
- 更新链接

**阶段 3 (自动化)**:
- 配置 Moltbot
- 启动监控
- 开始双周报告

## 注意事项

1. **保留历史**: 使用 Git 分支而非删除
2. **渐进迁移**: 不要一次性移动所有文件
3. **文档优先**: 先更新 AGENT.md 和 README，再改结构
4. **验证频繁**: 每个阶段都测试 Cursor 理解度
5. **社群通知**: 如果有其他贡献者，提前沟通

## 需要我帮助的地方

当你执行这个改造时，可以随时问我:
- "帮我生成 app_index.md 的初始内容"
- "这个文件应该移到哪个新目录?"
- "帮我更新这个文档中的所有链接"
- "生成迁移脚本"
- "验证新的 AGENT.md 是否符合标准"

---

**准备好开始了吗？从哪个任务开始？**
